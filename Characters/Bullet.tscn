[gd_scene load_steps=4 format=2]

[ext_resource path="res://assets/bullet/bullet.png" type="Texture" id=1]

[sub_resource type="GDScript" id=3]
script/source = "extends Area2D

var speed = null
var damage = null
var player = null

# Called when the node enters the scene tree for the first time.
func _ready():
	connect(\"body_entered\", self, \"_on_enemy_body_enter\")


func setup(_player, _damage, _position,_speed):
	player = _player
	damage = _damage
	position = _position
	speed = _speed

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	position += Vector2(speed*delta,0)

	

func kill_bullet():
	get_parent().remove_child(self)
	
func _on_enemy_body_enter(body):
	if \"Enemy\" in body.get_name():
		body.inflictDamage(damage, player)
		kill_bullet()
"

[sub_resource type="RectangleShape2D" id=2]
extents = Vector2( 23.255, 5.58579 )

[node name="bullet" type="Area2D" groups=[
"bullet",
]]
collision_layer = 2
collision_mask = 2
script = SubResource( 3 )

[node name="Sprite" type="Sprite" parent="."]
scale = Vector2( 0.357701, 0.357701 )
texture = ExtResource( 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="."]
position = Vector2( 1.26224, 1.5778 )
shape = SubResource( 2 )
